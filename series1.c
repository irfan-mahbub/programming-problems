#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    int sum = 0;
    // will add 1 to n in sum
    sum +=1;

    sum +=2;
    sum +=3;
    sum +=4;
    sum +=5;
    for(sum = 0;sum < 6; sum++);11
    printf("%d\n", sum);
}
